{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "ios",
  "projectType": "application",
  "targets": {
    "list": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "ios",
        "command": "xcodebuild -list -project maimaid.xcodeproj"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "ios",
        "command": "xcodebuild -project maimaid.xcodeproj -scheme maimaid -configuration Debug -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO clean"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "ios",
        "command": "xcodebuild -project maimaid.xcodeproj -scheme maimaid -configuration Debug -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO build"
      }
    }
  },
  "tags": [
    "platform:ios"
  ]
}
